
<div class="media">
 <div class="media-body">
   <h4 class="media-heading">
     <%= link_to wiki.title, wiki_path(wiki) %>
     <% if wiki.private? %>
       <small>private</small>
     <% else %>
       <small>public</small>
     <% end %>
   </h4>
 </div>
</div>